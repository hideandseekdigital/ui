---
"@hideandseekdigital/ui": minor
---

Initial component set built on shadcn/ui (latest) with the Tailwind v4 token pipeline:
Button, Card, Avatar, Badge, Checkbox, Dropdown Menu, Input, Select, Separator,
Slider, Switch, Table, Textarea, Tooltip. Includes Storybook stories, design tokens
(`styles/tokens.css`), and ESM + CJS + type outputs.
